# 多无人机三维编队与协同避障分阶段编码提示词

## 使用说明

本文档用于分阶段生成“基于人工势场法的多无人机三维编队保持与协同避障”仿真程序。

推荐严格按照下列顺序执行：

```text
步骤一：三维编队基础程序，对应实验一
                   ↓
步骤二：静态障碍物与机间避碰，对应实验二
                   ↓
步骤三：局部极小检测与切向逃逸，对应实验三
                   ↓
步骤四：移动障碍物预测避障，对应实验四
                   ↓
步骤五：统一入口、数据保存和最终验收
```

使用时注意：

1. 每次只向代码生成模型提交一个步骤的提示词；
2. 每完成一个步骤，先实际运行并检查输出，再进入下一步；
3. 后续步骤应在前一步的项目文件上继续修改；
4. 如果模型能够直接访问项目目录，应要求其直接编辑并运行验证；
5. 如果模型不能访问项目目录，应提供当前目录结构和相关源代码；
6. 不要在前一阶段尚未通过时同时添加多个新功能；
7. 程序使用 Python、NumPy、Matplotlib、Pandas 和 Pillow，不引入 ROS、Webots、AirSim 等复杂平台。

## 统一项目结构

```text
uav_swarm_3d/
├── main.py
├── config.py
├── models.py
├── controller.py
├── simulator.py
├── visualization.py
├── requirements.txt
├── README.md
└── results/
    ├── experiment_1/
    ├── experiment_2/
    ├── experiment_3/
    └── experiment_4/
```

---

## 步骤一提示词：三维编队基础程序

对应实验一：无障碍环境下的三维编队形成与保持，并完成姿态和螺旋桨动画基础。

```text
请使用Python创建一个“基于人工势场法的多无人机三维编队保持与协同避障”仿真项目。

这是第1步：实现无障碍环境下的三维编队形成与保持，同时实现难度适中的姿态跟随和螺旋桨视觉动画，对应实验一。

一、基本要求
1. 使用Python 3.10或更高版本。
2. 使用numpy和matplotlib。
3. 不使用ROS、Webots、AirSim和PyBullet。
4. 位置采用三维一阶运动学模型。
5. 模拟横滚角、俯仰角、偏航角、四旋翼机体和四个螺旋桨的旋转效果。
6. 不建立质量、惯性矩、推力、力矩、电机响应和空气动力学方程。
7. 螺旋桨转速只用于动画，不得反作用于无人机位置和姿态。
8. 代码必须完整、可运行，并包含必要的中文注释和docstring。
9. 使用dataclass定义无人机。
10. 使用固定随机种子，保证结果可复现。
11. 不要只提供伪代码，应提供所有文件的完整代码。

二、项目结构
创建：
main.py
config.py
models.py
controller.py
simulator.py
visualization.py
requirements.txt

results目录由程序自动创建。

三、无人机运动模型
无人机状态包括：
- id；
- position，numpy数组，shape为(3,)；
- velocity，numpy数组，shape为(3,)；
- attitude，依次为roll、pitch、yaw，shape为(3,)；
- desired_attitude，shape为(3,)；
- last_valid_yaw；
- rotor_speeds，4个视觉旋翼转速，shape为(4,)；
- rotor_phases，4个旋翼相位，shape为(4,)；
- history，用于保存历史位置；
- is_leader。

位置更新：
p(t+dt)=p(t)+v(t)*dt。

速度平滑：
v(t+dt)=(1-alpha)*v(t)+alpha*v_cmd(t)。

速度模长不得超过v_max。如果超过上限，应保持方向不变并限制其模长，不能分别截断三个坐标分量。

明确说明：上述位置和速度更新不使用旋翼推力。rotor_speeds仅用于可视化。

四、编队设置
共5架无人机：
- 0号为领航者；
- 1～4号为跟随者。

三维楔形编队偏移量：
leader=[0,0,0]
uav1=[-5,3,2]
uav2=[-5,-3,2]
uav3=[-5,3,-2]
uav4=[-5,-3,-2]

领航者初始位置：
[0,0,12]

目标点：
[50,0,12]

跟随者从各自期望编队位置附近随机初始化，每个坐标加入[-2,2]米扰动，random_seed=42。

五、控制算法
领航者：
v_cmd=k_goal*(goal-position)。

跟随者：
v_cmd=k_form*(desired_position-position)+k_vel*(leader_velocity-velocity)。

根据领航者水平速度计算航向角：
yaw=atan2(v_y,v_x)。

使用绕z轴旋转矩阵旋转编队偏移量：
desired_position=leader_position+Rz(yaw)@offset。

领航者水平速度接近0时，保留上一个有效航向角，不能直接重置为0。

六、姿态跟随模型
姿态采用roll、pitch、yaw欧拉角表示，但不建立姿态动力学方程。

1. 使用期望速度变化计算用于显示的近似期望加速度：
a_des=(v_cmd-velocity)/tau_v。

2. 期望偏航角：
yaw_des=atan2(v_y,v_x)。
优先使用当前平滑速度方向；水平速度过小时保持last_valid_yaw。

3. 使用Rz(-yaw_des)将a_des的水平分量旋转到机体坐标系，得到a_body_x和a_body_y。

4. 近似期望俯仰角和横滚角：
pitch_des=clip(a_body_x/g,-max_pitch,max_pitch)
roll_des=clip(-a_body_y/g,-max_roll,max_roll)

5. max_roll和max_pitch均设置为25度，程序内部统一使用弧度。

6. 姿态平滑跟随：
roll += beta_rp*(roll_des-roll)
pitch += beta_rp*(pitch_des-pitch)
yaw += beta_yaw*wrap_to_pi(yaw_des-yaw)

7. 实现wrap_to_pi函数，将角度误差限制在[-pi,pi]，避免偏航角跨越正负180度时反向大角度旋转。

七、螺旋桨视觉模型
每架无人机具有4个机臂和4个旋翼，使用X形或十字形布局，但整个项目必须保持统一。

1. 设置视觉基础转速：
omega_base=omega_hover_visual+k_rotor_z*v_cmd_z。

2. 根据姿态误差为4个旋翼增加小幅差动转速。可以使用简化混控矩阵，但必须在注释中说明该混控只服务于视觉效果。

3. 将视觉转速限制在：
omega_min_visual <= rotor_speeds <= omega_max_visual。

4. 相邻旋翼使用相反旋转方向，例如：[1,-1,1,-1]。

5. 更新旋翼相位：
phase += direction*rotor_speed*dt。
相位使用模2*pi处理，避免数值无限增大。

6. rotor_speeds不得用于更新无人机的位置、速度或姿态。

八、三维四旋翼绘制
在visualization.py中：
1. 根据roll、pitch、yaw构造旋转矩阵R=Rz(yaw)@Ry(pitch)@Rx(roll)；
2. 在机体局部坐标系定义4个机臂端点；
3. 使用旋转矩阵将机臂和旋翼位置转换到世界坐标系；
4. 领航者机体使用红色，跟随者使用不同颜色；
5. 用短线段、十字线或半透明小圆盘表示旋翼；
6. 旋翼线段方向应随rotor_phases变化；
7. 坐标轴范围固定，避免动画抖动；
8. 动画显示可以与仿真计算解耦，先计算轨迹和姿态历史，再播放动画。

九、默认参数
dt=0.05秒；
max_time=100秒；
v_max=3.0米/秒；
alpha=0.2；
k_goal=0.5；
k_form=0.6；
k_vel=0.3；
tau_v=0.5秒；
g=9.81米/秒平方；
max_roll=25度；
max_pitch=25度；
beta_rp=0.15；
beta_yaw=0.10；
omega_hover_visual=45弧度/秒；
omega_min_visual=20弧度/秒；
omega_max_visual=80弧度/秒；
goal_tolerance=1.0米。

十、实验一
比较3组编队增益：
k_form=0.3、0.6、1.0。

每组实验输出：
- 是否到达目标；
- 任务完成时间；
- 全过程平均编队误差；
- 最大编队误差；
- 最终编队误差；
- 每架无人机飞行距离；
- 集群总飞行距离；
- 最大横滚角；
- 最大俯仰角；
- 姿态变化均方根；
- 视觉旋翼转速范围。

生成：
1. 三维飞行轨迹图；
2. 编队误差曲线；
3. 三种k_form对应的误差对比图；
4. 领航者和一架跟随者的roll、pitch、yaw曲线；
5. 代表性无人机的4个视觉旋翼转速曲线；
6. 一份能够显示机体倾斜和螺旋桨旋转的GIF动画。

结果保存到：
results/experiment_1/

十一、验收条件
1. 领航者可以到达目标；
2. 跟随者能够逐渐形成三维楔形编队；
3. 最终平均编队误差应小于1米；
4. 所有无人机速度不超过v_max；
5. roll和pitch不超过设定角度上限；
6. 偏航角跨越正负pi时没有突跳；
7. 位置、速度、姿态、旋翼转速和旋翼相位中不出现NaN或Inf；
8. 四个旋翼相位随时间连续变化，相邻旋翼方向相反；
9. 螺旋桨转速不会影响无人机位置运动结果；
10. 关闭姿态和旋翼绘制后，相同随机种子的轨迹结果保持不变；
11. 不能通过直接赋值位置实现编队；
12. 仿真计算与绘图代码分离；
13. 程序能够通过python main.py运行。

请先给出项目目录结构，再逐个给出所有文件的完整代码。代码完成后检查import路径、数组形状、速度限幅、姿态限幅、角度环绕、旋翼相位和结果目录创建，并说明运行命令。不要将该模型描述为四旋翼飞行动力学模型。
```

### 步骤一检查清单

- 领航者能够到达目标；
- 跟随者实际经过控制运动到期望位置，而不是被直接设置到期望位置；
- 编队偏移量能随领航者航向旋转；
- 编队误差整体呈下降趋势；
- 机体倾斜方向与转弯、加速趋势基本一致；
- 偏航角没有正负 180° 跳变；
- 四个旋翼具有独立相位且相邻旋转方向相反；
- 关闭动画不会改变运动轨迹；
- 图像能够保存到 `results/experiment_1/`。

---

## 步骤二提示词：静态障碍物与机间避碰

对应实验二：单个静态障碍物环境下的协同避障。

```text
请在现有uav_swarm_3d项目上完成第2步开发。

请先阅读现有代码，保留已经正确实现的三维编队、速度平滑、速度限幅、姿态跟随、旋翼视觉动画和评价指标。不要无故重写已经验证通过的功能。

本步骤加入球形静态障碍物、人工势场避障和无人机间防碰撞，对应实验二。

一、静态障碍物
使用dataclass定义SphereObstacle，包含：
- obstacle_id；
- center；
- radius；
- influence_distance。

默认障碍物：
center=[25,0,12]
radius=4.0米
influence_distance=6.0米

无人机等效半径：
uav_radius=0.3米。

二、障碍物排斥力
无人机到障碍物表面的净距离：
rho=norm(position-center)-radius-uav_radius。

当rho<influence_distance时：
F_obs=k_obs*(1/rho-1/rho_0)*(1/rho^2)*direction。

其中：
direction=(position-center)/norm(position-center)。

当rho>=influence_distance时，排斥力为零。

程序必须：
1. 对rho和方向向量分母设置epsilon下限，防止除零；
2. 对排斥力设置最大模长；
3. 支持将多个障碍物的排斥力相加；
4. 影响范围外严格返回shape为(3,)的零向量。

建议参数：
k_obs=8.0；
max_obstacle_force=5.0；
distance_epsilon=0.05。

三、无人机间防碰撞
当两架无人机距离小于d_safe时，产生机间排斥力：
F_sep=k_sep*(1/d-1/d_safe)*(1/d^2)*direction。

建议参数：
d_safe=2.0米；
k_sep=3.0；
max_separation_force=4.0。

如果两架无人机位置完全重合，必须使用基于无人机编号的确定性方向将其分开，不能生成随机方向或NaN。

四、控制算法
领航者：
v_cmd=F_goal+F_obs+F_sep。

跟随者：
v_cmd=F_form+F_vel+F_obs+F_sep。

所有控制分量相加后仍然需要执行速度限幅和平滑更新。

避障控制仍然只生成期望速度。随后沿用步骤一的方法，根据期望速度变化生成近似姿态，再更新视觉旋翼转速和相位。不得把人工势场力直接转换为真实旋翼推力，也不得加入飞行动力学方程。

五、碰撞检测
实现：
1. 无人机与障碍物碰撞检测；
2. 无人机之间碰撞检测。

当：
norm(position-center)<obstacle_radius+uav_radius
时，判定与障碍物碰撞。

当：
distance_between_uavs<2*uav_radius
时，判定无人机间碰撞。

同一次连续接触只记录一次碰撞，不能在每个时间步重复计数。无人机对(i,j)和(j,i)必须视为同一个碰撞对。

六、实验二
比较3种方法：
A：不使用障碍物避障；
B：仅使用障碍物人工势场；
C：障碍物人工势场+无人机间防碰撞。

三种方法必须使用相同初始位置。

输出：
- 是否成功；
- 任务完成时间；
- 平均编队误差；
- 最大编队误差；
- 障碍物最小净距离；
- 最小机间距离；
- 障碍物碰撞次数；
- 无人机碰撞次数；
- 集群总飞行距离；
- 避障过程最大横滚角和最大俯仰角；
- 姿态达到显示上限的次数。

生成：
1. 三种方法的三维轨迹图；
2. 编队误差对比图；
3. 障碍物最小净距离曲线；
4. 最小机间距离曲线；
5. 实验结果对比表。

结果保存到：
results/experiment_2/

七、三维绘图
球形障碍物使用灰色半透明球面表示。
领航者使用红色，跟随者使用不同颜色。
图中显示目标点和所有无人机轨迹。
代表性动画继续显示四旋翼机臂、机体倾斜和4个旋翼的独立转动。

八、验收条件
1. 关闭避障时能够正确检测碰撞；
2. 开启人工势场后无人机能够绕过障碍物；
3. 排斥力方向必须背离障碍物；
4. 开启机间避碰后最小机间距离应有所提高；
5. 不允许通过直接修改位置实现避障；
6. 不出现速度超限、NaN或Inf；
7. 姿态角保持在设定范围，偏航角无跨界突跳；
8. 避障引起的机体倾斜应平滑，不能瞬间翻转；
9. 旋翼动画不得改变运动轨迹；
10. 不能破坏实验一的运行功能。

请直接修改当前项目。完成后给出新增或修改的文件、验证结果，以及实验一和实验二的运行命令。如果不能直接访问项目文件，请给出所有新增或修改文件的完整代码。
```

### 步骤二检查清单

- 障碍物影响范围外的排斥力为零；
- 排斥力指向远离障碍物的方向；
- 关闭避障时仍保留碰撞检测；
- 开启避障后没有使用位置瞬移；
- 同一次碰撞没有被连续重复计数。

---

## 步骤三提示词：局部极小检测与切向逃逸

对应实验三：多障碍物环境下标准与改进人工势场法对比。

```text
请在现有uav_swarm_3d项目上完成第3步开发。

本步骤研究标准人工势场法的局部极小问题，并加入切向逃逸力，对应实验三。

请保留已有的编队、静态障碍物避障、机间防碰撞、姿态跟随、旋翼视觉动画和碰撞检测功能，不要大范围重写已经验证正确的代码。

一、多障碍物场景
设置4个球形障碍物：

obstacle_1:
center=[20,-4,12]
radius=3.5

obstacle_2:
center=[20,4,12]
radius=3.5

obstacle_3:
center=[27,0,9]
radius=3.0

obstacle_4:
center=[27,0,15]
radius=3.0

目标点：
[50,0,12]。

二、局部极小判断
为每架无人机记录最近20步的：
- 速度大小；
- 领航者到目标点的距离；
- 跟随者到期望编队位置的距离。

建议使用collections.deque保存固定长度历史数据。

当同时满足以下条件时，认为无人机可能陷入局部极小：
1. 到目标或期望位置的距离大于2米；
2. 当前速度小于0.15米/秒；
3. 附近存在影响范围内的障碍物；
4. 最近20步的距离改善小于0.05米。

局部极小条件应连续满足若干步后再触发，不能只根据一个时间步判断。

三、切向逃逸力
找到距离无人机最近的障碍物。

计算径向单位向量：
n=(position-obstacle_center)/norm(position-obstacle_center)。

计算切向方向：
t=cross(n,[0,0,1])。

如果t的模长过小，则使用：
t=cross(n,[1,0,0])。

将t归一化：
F_escape=k_escape*t。

建议参数：
k_escape=1.0；
escape_max_duration=2.0秒。

为了避免不同无人机随机选择相反绕行方向，使用基于无人机编号和领航者绕行方向的确定性规则选择切向符号，保证实验可复现。

当无人机重新取得明显的目标距离改善时，退出逃逸状态。

四、实验三
比较：
1. standard_apf：标准人工势场法；
2. improved_apf：人工势场法+切向逃逸力。

两种方法必须使用相同初始位置和相同障碍物。

使用10个固定随机种子重复实验。批量实验不显示动画。

任务成功条件：
- 最大时间内到达目标；
- 没有发生无人机间碰撞；
- 没有发生障碍物碰撞；
- 最终平均编队误差小于1米。

记录：
- 成功率；
- 平均任务完成时间；
- 平均总飞行距离；
- 平均和最大编队误差；
- 最小障碍物净距离；
- 局部极小检测次数；
- 逃逸力启用次数；
- 碰撞次数；
- 逃逸阶段最大横滚角和俯仰角；
- 姿态变化均方根。

五、输出
保存：
results/experiment_3/all_runs.csv
results/experiment_3/summary.csv

生成：
1. 标准方法三维轨迹图；
2. 改进方法三维轨迹图；
3. 成功率柱状图；
4. 平均任务时间对比图；
5. 平均飞行距离对比图；
6. 编队误差对比图。

六、验收条件
1. 关闭改进功能时不能产生逃逸力；
2. 两种方法必须使用完全相同的初始条件；
3. 局部极小不能仅根据单步速度判断；
4. 切向向量必须归一化；
5. 不能通过瞬移实现逃逸；
6. 单次实验失败不能导致批量实验中断；
7. 标准方法失败时也必须保存实验数据；
8. 切向逃逸引起的姿态变化必须经过平滑跟随，不能直接修改姿态；
9. 螺旋桨转速继续只用于动画，不参与逃逸控制；
10. 不预设改进方法一定成功，结果必须由仿真真实计算。

如果当前障碍物配置不能体现局部极小问题，允许少量调整障碍物位置或人工势场参数，但必须将最终参数写入config.py，并在代码注释和结果配置中说明。

请直接修改当前项目。完成后说明修改内容、验证结果，以及单次实验和10次重复实验的运行命令。如果不能直接访问项目文件，请给出所有新增或修改文件的完整代码。
```

### 步骤三检查清单

- 标准方法和改进方法使用完全相同的初始条件；
- 局部极小判定使用一段时间的历史数据；
- 逃逸力只在满足条件时短时间启用；
- 所有批量实验均记录随机种子；
- 失败实验同样出现在 CSV 结果中。

---

## 步骤四提示词：移动障碍物预测避障

对应实验四：移动障碍物环境下当前位置避障与预测避障对比。

```text
请在现有uav_swarm_3d项目上完成第4步开发。

本步骤增加移动球形障碍物，并比较当前位置避障和预测位置避障，对应实验四。

请保留前三个实验的所有功能和运行入口，包括姿态跟随与旋翼视觉动画。

一、移动障碍物
使用dataclass定义MovingSphereObstacle，包含：
- obstacle_id；
- center；
- radius；
- velocity；
- influence_distance；
- history。

每个仿真步更新：
center=center+velocity*dt。

默认移动障碍物：
center=[25,-12,12]
radius=2.0
velocity=[0,1.0,0]

即障碍物沿y轴正方向横穿无人机集群路径。

二、预测避障
增加配置参数：
enable_prediction；
prediction_time=1.0秒。

关闭预测时，使用障碍物当前位置计算人工势场：
predicted_center=center。

开启预测时：
predicted_center=center+prediction_time*velocity。

控制器使用predicted_center计算排斥力。

碰撞检测必须使用移动障碍物的真实当前位置，不能使用预测位置。

三、时间步执行顺序
每个仿真步明确执行：
1. 读取当前无人机和障碍物状态；
2. 计算移动障碍物预测位置；
3. 计算所有无人机控制速度；
4. 更新无人机状态；
5. 根据速度变化更新无人机姿态、视觉旋翼转速和旋翼相位；
6. 更新移动障碍物真实位置；
7. 使用真实位置进行碰撞检测；
8. 记录位置、速度、姿态、旋翼和安全指标。

如果现有程序采用其他一致的更新顺序，可以保留，但必须在代码注释中说明。

四、实验四
分别测试移动障碍物速度：
0.5米/秒；
1.0米/秒；
1.5米/秒。

比较两种方法：
1. current_position：使用障碍物当前位置避障；
2. predicted_position：使用预测位置避障。

每个组合使用5个相同的固定随机种子重复实验。

五、评价指标
记录：
- 是否成功；
- 任务完成时间；
- 无人机与移动障碍物的最小净距离；
- 最小机间距离；
- 平均编队误差；
- 最大编队误差；
- 编队恢复时间；
- 集群总飞行距离；
- 碰撞次数；
- 动态避障阶段最大横滚角和俯仰角；
- 姿态变化均方根。

编队恢复时间定义为：
避开移动障碍物后，平均编队误差重新小于0.8米，并连续保持2秒。

如果仿真结束仍未恢复，保存为NaN，并在汇总结果中统计未恢复次数。

六、三维动画
选择一个代表性场景生成动画：
- 领航者使用红色；
- 跟随者使用不同颜色；
- 移动障碍物使用橙色半透明球体；
- 显示移动障碍物历史轨迹；
- 显示无人机历史轨迹；
- 显示无人机机体姿态、4个机臂和4个旋翼；
- 旋翼线段或圆盘随各自相位旋转；
- 显示目标点；
- 标题实时显示时间、编队误差和最小净距离。

支持保存GIF。如果GIF保存失败，仍然必须保存实验数据和静态轨迹图。

七、输出
保存：
results/experiment_4/all_runs.csv
results/experiment_4/summary.csv

生成：
1. 无预测避障三维轨迹图；
2. 预测避障三维轨迹图；
3. 不同移动速度下的成功率图；
4. 最小净距离对比图；
5. 最大编队误差对比图；
6. 编队恢复时间对比图；
7. 一份代表性GIF动画。

八、验收条件
1. 移动障碍物必须按设定速度更新；
2. 预测功能只能影响避障控制，不能修改障碍物真实轨迹；
3. 碰撞检测必须使用真实位置；
4. 两种方法必须使用相同初始条件；
5. 每次实验开始时重新创建无人机和障碍物；
6. 批量实验时不显示动画；
7. 不出现NaN、Inf和速度超限；
8. 姿态平滑且不超过设定角度范围；
9. 旋翼动画不得改变预测避障结果；
10. 不破坏前三个实验。

请直接修改当前项目。完成后说明新增或修改内容、验证结果，以及实验四的单次动画运行命令和批量实验命令。如果不能直接访问项目文件，请给出所有新增或修改文件的完整代码。
```

### 步骤四检查清单

- 移动障碍物的位移与速度、仿真时间一致；
- 预测位置仅用于控制器；
- 真实位置用于碰撞检测和真实轨迹绘制；
- 两种方法使用相同的初始条件与随机种子；
- 批量实验不会打开大量绘图窗口。

---

## 步骤五提示词：项目整合与最终验收

本步骤不新增实验，只整合前四个实验。

```text
请对现有uav_swarm_3d项目进行最终整合。

项目已经包含4个实验：
1. 三维编队形成与保持；
2. 单个静态障碍物协同避障；
3. 标准与改进人工势场法对比；
4. 移动障碍物预测避障。

本步骤不增加新的控制算法，只完成统一入口、数据保存、绘图、基本测试和使用文档。不要为了重构而改变已经验证正确的控制结果。

一、统一运行入口
main.py使用argparse支持：

python main.py --experiment 1
python main.py --experiment 2
python main.py --experiment 3
python main.py --experiment 4
python main.py --experiment all

可选参数：
--seed 42
--animate
--batch-runs 10

二、统一实验结果
每次实验至少保存：
- metrics.csv；
- summary.json；
- trajectory.npz；
- trajectory_3d.png；
- formation_error.png；
- safety_distance.png；
- attitude.png；
- rotor_speed.png。

trajectory.npz中保存位置、速度、姿态、期望姿态、视觉旋翼转速和旋翼相位历史。不要把长轨迹全部写入JSON。

JSON保存前将numpy数组、numpy整数、numpy浮点数和布尔值转换为Python原生类型，避免序列化错误。

三、统一评价指标
四个实验尽量统一使用：
- 成功或失败；
- 失败原因；
- 任务完成时间；
- 平均编队误差；
- 最大编队误差；
- 最终编队误差；
- 最小机间距离；
- 最小障碍物净距离；
- 每架无人机飞行距离；
- 集群总飞行距离；
- 无人机碰撞次数；
- 障碍物碰撞次数；
- 最大横滚角；
- 最大俯仰角；
- 姿态变化均方根；
- 姿态达到显示上限的次数；
- 视觉旋翼最小和最大转速。

失败原因至少区分：
none、timeout、uav_collision、obstacle_collision、formation_not_recovered、numerical_error。

如果某次实验出现NaN或Inf，应终止该次实验并记录numerical_error，不能导致整个批量实验崩溃。

四、三维可视化
统一绘图风格：
- 领航者为红色；
- 跟随者使用不同颜色；
- 静态障碍物为灰色半透明球体；
- 移动障碍物为橙色半透明球体；
- 目标点使用星形；
- 根据roll、pitch、yaw绘制四旋翼机体方向；
- 显示4个机臂和4个旋翼；
- 旋翼线段或圆盘方向随rotor_phases变化；
- 相邻旋翼显示相反旋转方向；
- 显示图例、坐标轴名称和单位；
- 固定坐标范围；
- 保存后关闭figure，避免批量实验占用过多内存。

五、基本测试
不需要建立复杂测试体系，只需使用test_basic.py或assert检查：
1. 速度限幅后速度模长不超过v_max；
2. 障碍物影响范围外排斥力为0；
3. 障碍物排斥力方向远离障碍物；
4. 机间排斥力方向使两架无人机相互远离；
5. 无人机和障碍物碰撞检测正确；
6. 相同随机种子得到相同初始位置和结果；
7. roll和pitch不超过设定上限；
8. wrap_to_pi对正负pi边界处理正确；
9. 旋翼相位始终位于[0,2*pi)；
10. 视觉旋翼转速处于设定范围；
11. 开启或关闭旋翼动画不改变运动轨迹；
12. 仿真中不出现NaN或Inf。

六、README
创建README.md，说明：
- 项目功能；
- Python版本；
- 依赖安装；
- 四个实验的研究内容；
- 四个实验的运行命令；
- 主要参数；
- 输出文件；
- 简化假设；
- 当前模型的局限性；
- 明确说明姿态和旋翼用于运动学可视化，不是飞行动力学仿真，视觉旋翼转速不参与运动计算。

七、依赖
requirements.txt尽量只保留项目实际使用的依赖：
numpy
matplotlib
pandas
pillow

八、最终验收
实际运行并检查：

pip install -r requirements.txt
python main.py --experiment 1
python main.py --experiment 2
python main.py --experiment 3 --batch-runs 10
python main.py --experiment 4 --batch-runs 5
python main.py --experiment all

重点检查：
1. 所有import路径正确；
2. results目录自动创建；
3. 四个实验的结果不会互相覆盖；
4. 批量实验不会打开大量动画窗口；
5. 同一次碰撞不会在每个时间步重复计数；
6. 每次实验都会重新初始化无人机和障碍物；
7. 不出现除零、NaN和Inf；
8. 相同随机种子能够复现实验；
9. 姿态限幅、偏航角环绕和旋翼相位更新正确；
10. 关闭动画后轨迹和实验指标不变；
11. README中的命令与实际程序一致。

如果发现问题，请直接修复并重新验证。不要声称命令已经成功运行，除非确实执行过。

最终请给出：
- 完整项目目录；
- 新增或修改文件；
- 实际运行的验证命令；
- 基本测试结果；
- 四个实验的运行方法；
- 当前项目的已知限制。
```

### 步骤五检查清单

- 四个实验均可通过统一入口运行；
- 单次实验和批量实验的结果不会覆盖；
- 实验配置、随机种子和结果指标均被保存；
- 位置、速度、姿态和旋翼历史均可从NPZ重新加载；
- 文档没有把视觉旋翼模型描述成真实推力或飞行动力学；
- GIF 保存失败不会造成实验数据丢失；
- README 中的命令可以直接复制执行。

---

## 通用报错修复提示词

如果程序运行时报错，将完整 Traceback 和以下提示词一起提交：

```text
下面是当前多无人机三维仿真项目运行时的完整报错。请先根据Traceback定位根本原因，再修改相关代码，不要只在报错行添加临时补丁。

修复要求：
1. 保留当前项目结构和已经实现的功能；
2. 不得通过删除编队、避障、姿态、旋翼、指标或绘图功能来规避问题；
3. 检查函数定义、调用参数和返回值是否一致；
4. 检查numpy数组shape：单个三维向量为(3,)，rotor_speeds和rotor_phases为(4,)，位置与姿态历史为(time,uav,3)，旋翼历史为(time,uav,4)；
5. 检查Python列表与numpy数组是否错误混用；
6. 检查是否存在除零、NaN或Inf；
7. 检查dataclass是否错误使用可变默认值；
8. 检查路径、results目录和文件编码；
9. 检查动画回调函数和Matplotlib对象生命周期；
10. 检查偏航角是否使用wrap_to_pi，姿态角是否正确限幅；
11. 检查旋翼相位是否模2*pi、相邻旋翼方向是否相反；
12. 确认视觉旋翼转速没有参与位置、速度或姿态更新；
13. 检查批量实验是否正确重置无人机和障碍物；
14. 修复后实际运行最小复现实验；
15. 给出受影响文件、修改原因和验证命令；
16. 不要声称已经运行成功，除非确实执行过。

完整报错：
【在这里粘贴Traceback】

运行命令：
【在这里粘贴命令】

相关配置或代码：
【在这里粘贴相关内容，或者让代码工具直接读取当前项目】
```

## 实验结果异常时的调参提示词

如果程序能够运行，但出现振荡、碰撞、无法到达目标或编队无法恢复，可使用：

```text
当前多无人机三维仿真程序能够运行，但实验结果存在异常。请先分析轨迹、指标和参数之间的关系，不要立即大范围重写代码。

异常现象：
【填写：例如无人机在障碍物前停止、编队振荡、跟随者碰撞、预测避障过早等】

当前参数：
【粘贴config.py中的参数】

当前实验指标：
【粘贴summary.json、CSV结果或控制台输出】

请重点检查：
1. 目标吸引力是否过小或过大；
2. 障碍物排斥力是否数值爆炸；
3. 编队保持力与避障力是否相互冲突；
4. 机间排斥力是否过强；
5. 速度平滑系数是否导致响应过慢；
6. 障碍物影响范围是否合理；
7. 最大排斥力和最大速度是否合理；
8. 是否陷入人工势场局部极小；
9. 编队期望位置是否随领航者航向正确旋转；
10. 移动障碍物预测时间是否过大或过小；
11. 姿态加速度时间常数tau_v是否导致倾斜过大；
12. beta_rp和beta_yaw是否导致姿态突变或响应过慢；
13. 偏航角误差是否正确进行[-pi,pi]环绕；
14. 旋翼视觉转速是否仅影响动画。

请输出：
1. 可能原因的优先级排序；
2. 每个判断所依据的指标或轨迹特征；
3. 推荐调整的参数及原因；
4. 一次只调整哪些参数；
5. 修改后的参数建议；
6. 必要时给出最小代码修改；
7. 用哪些指标判断问题是否解决。
```

## 最终实验与编码对应关系

| 开发步骤 | 对应实验 | 核心功能 | 主要输出 |
|---|---|---|---|
| 步骤一 | 实验一 | 三维编队、姿态跟随和旋翼动画 | 轨迹、编队误差、姿态、旋翼GIF |
| 步骤二 | 实验二 | 静态障碍物与机间避碰 | 最小距离、碰撞次数、避障姿态 |
| 步骤三 | 实验三 | 局部极小检测与切向逃逸 | 成功率、逃逸次数、姿态变化 |
| 步骤四 | 实验四 | 移动障碍物预测避障 | 动态安全距离、恢复时间、四旋翼GIF |
| 步骤五 | — | 项目整合与验收 | 统一入口、结果文件、README |

以上 5 个步骤构成完整的编码工作流，不再增加额外实验。每个阶段都应先完成最小可运行验证，再继续下一阶段。
